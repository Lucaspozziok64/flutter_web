import 'package:flutter/material.dart';

class VideoCart extends StatelessWidget {
  const VideoCart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Video 1"),
    );
  }
}